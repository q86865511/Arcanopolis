# PROGRESS — Arcanopolis

## 目前狀態

**美術方向已定案（2026-08-25）：以世紀帝國 II／Stronghold 為北極星，留在 2D tile 架構**。
經過一整輪評估（2D 階梯 tile → 山體 sprite → 純 3D → 3D 像素風 → 向量多邊形地形 →
中世紀城鄉版，六個方向各有 spike 與截圖，全部 commit 在 repo），使用者最終對齊到
AoE2 式的 2D 預算圖感。**關鍵結論：現有 tile 架構就是 AoE2 的技術構成，差距不在架構
在素材定調與密度**——待辦區有完整的「AoE2 五波計畫」，交由下一個 session 執行。
703 tests 綠、tsc 零錯誤。`spike-3d/` 與 `spike-terrain2d/` 保留為評估記錄（路線不採用，
但 3D spike 校準出的山地拉抬係數與 2.5D 手法日後可參考）。

**原玩法路線（美術五波完成後接續）**：M5 看得見的城市（W2 資源列診斷／W3 建築選單／
W4 日夜視覺）→ M6 道路與城市規劃（完整設計已在
`C:\Users\q86865511\.claude\plans\c-users-q86865511-appdata-local-temp-cl-starry-flute.md`）
→ M7 居民要什麼 → M8 危機事件＋塔防 → M9 公會 → M10 魔法 → M11 種族 → M12 上架。

## 已完成

- [2026-08-25] **AoE2 美術 W1 定調重生**（typecheck/703 tests/build 全綠）。
  art-bible 新增「AoE2 北極星定調」段（暗橄欖/高雜色/寫實偏暗），地形與建築前綴
  補色調指令（DARK REALISTIC TONE 段）。兩輪生圖共 9 張：grass×3（01 走到 v5、02 v3、
  03 v5）、forest×2（v6/v5）、water/ore 順手重生（舊版純色→柔和色斑）。
  第一輪 grass-01 的孤立亮斑與 grass-03 的單一大團塊鋪排成陣列，第二輪以
  「亮部柔和連續／小型多樣均佈」修正通過。中心均色 (109,172,52)→(84,89,39)。
  Minimap 代表色同步取新素材實際均色（TerrainRenderer 的 TERRAIN_TINT 查為死碼未動）。
  驗收：verify-terrain-tiles（grass-02/03、forest×2 過標，grass-01 11.6 略低但 5×5
  目視通過）、verify-terrain-seams holes=0、實機截圖前後對比交使用者確認色調中。
- [2026-08-25] **美術方向評估與定案：AoE2 北極星**（703 tests 全程綠）。使用者以 Anno 2070
  截圖提出「山要有真坡度、房與船要立體」，一路評估六個方向、每個都有實跑 spike 與截圖：
  (1) 2D 階梯 tile（已實裝，被評「還是平的」）；(2) 2D 大型山體 sprite（合成 mockup，
  被評不夠）；(3) Three.js 純 3D（用同一份 core 資料渲染，證明 `src/core` 零改動可餵 3D、
  703 tests 原樣綠——架構鐵則的實證）；(4) 3D 像素風（RenderPixelatedPass＋保色相亮度量化，
  教訓：量化必須「同色相分明度階」，linear 空間逐通道取整會生出假色）；(5) 向量多邊形地形
  （marching squares＋Chaikin＋2.5D 高程＋坡向光照，教訓：光照必須是連續場，逐格筆刷會留
  點狀花紋）；(6) 中世紀城鄉版（田野拼布/泥土路/現有素材組城寨）。
  **最終定案**：以世紀帝國 II／Stronghold 為北極星留在 2D tile 架構——AoE2 本身就是
  菱形 tile 遊戲，現有架構（tile＋變體＋過渡＋裝飾＋階梯高度）正是其技術構成，
  差距只在素材定調與密度。五波執行計畫在待辦區。
  spike 保留於 `spike-3d/`、`spike-terrain2d/`（含 `npm i three`）供日後參考。

- [2026-08-24] **全島階梯地形**（703 tests，+12）。島嶼不再是全平地：依既有高程量化成
  0–3 階（水/沙灘 0、平原 1、丘陵 2、山地 3，seed=1 實測 21.7%/20.8%/2.6%），
  階地交界露出壓暗的土壁裙邊。
  **關鍵發現**：高程值在 `baseTerrainAt` 裡本來就算出來了（地形分類就是從它切的），
  抽出 `elevationValueAt` 共用後，「山一定比平原高、水一定最低」是代數保證。
  丘陵切分 0.64 **刻意不對齊任何地形門檻**——草地橫跨 1/2 階，階地邊緣才會穿過草原。
  fBm 連續性保證相鄰格最多跳一階（已鎖進測試），裙邊只需支援單階高度。
  **架構**：高度是純視覺屬性（core 玩法零改動、存檔零改動、balance 曲線不變）；
  `elevationLevelAt` 只吃 seed 不吃 override，砍樹不會讓地面升降。
  render 端：tile 上移 `level×8px`、裙邊用壓暗的 dirt tile 疊層；chunk RT 上下加
  padding 並把繪製範圍外擴一格，交界區由兩側 chunk 各自重複繪製同一格，
  跨 RT 遮擋不依賴 RenderTexture 的加入順序。
  **拾取修正**（`pickElevatedTile`）：階地把頂面畫在平面投影之上，平面 `hitTile` 會
  選到視覺格的後方格——quarry/mine 要蓋在 rock 上而 rock 分布在 1/2 階，不修會放錯位。
  由高階往低階枚舉「hitTile(y+L×8) 且該格恰為 L 階」，視覺在前者先命中。
  居民高度用雙線性插值（0.1 格步進下沿斜坡走而非瞬跳 8px）。

- [2026-08-24] **地形素材三個結構性缺陷修正**（批次 1 ＋ 2a，691 tests 綠）。
  實機截圖診斷指出瓶頸在地形而非建築：草地是純色平面、山地鋪成規律山峰陣列、交界是硬邊。
  **art-bible 的地形前綴改三處**：強化無描邊（舊版寫了 `no outline` 但 AI 照樣加黑邊）、
  新增地表結構要求（講明 24 倍降採樣與 50px 門檻——raw 上 50px 才等於成品 2px，
  不講清楚 AI 會畫細雜訊，而細雜訊降採樣後平均回純色）、新增無居中主體要求。
  **實測校準的驗收標準**（寫回 art-bible）：中央標準差 12–18；但**決定性的是三條質性規則**
  ——禁方向性紋理（帶／層／浪／溝，鋪排會連成貫穿地圖的斜條紋）、禁高對比離散特徵物
  （綠草上的橘土斑會變成一眼認出的地標陣列）、目標形態是「同色系有機團塊的均勻地毯」。
  這三條是拿 18 張生圖換來的：第一輪 10 張只有 grass-02 過關，兩種失敗模式都是我在
  prompt 裡自己要求的（「橫向岩層帶」「帶狀起伏」「淺溝」）。
  **成果**：中央標準差 2.2–2.9 → 12–18；mountain 亮度 55.4 → 129.7–141.5 且山峰陣列消失；
  森林從深綠噪點變成讀得出樹冠。新增變體 grass-03／forest-02／sand-02／rock-02／
  mountain-02／mountain-03 供渲染層日後打散重複感。
  **管線兩項改進**：邊緣填補從「灌中央平均色」改為「把內容向外拉伸」——前者讓每格都有一圈
  平均色鑲邊，鋪排時連成格線（實測 grass-01 邊緣比中央亮 7 階，改後降到 4）；
  `tileNames` 支援 `{raw, out}` 映射，讓 `assets\raw` 永不覆蓋與「重生素材要落到現用檔名」
  兩個要求並存。新增 `scripts\verify-terrain-tiles.mjs`（量化指標＋5×5 鋪排圖）。
  **未達標登記**：forest-01 的樹冠在實機大視野下仍有可見陣列（待重生）；
  山地移除主體後變得很平、失去體積感，那要靠批次 2b 的山體疊加 sprite 補，
  而 2b 與地形過渡（批次 4）都卡在渲染層規格未定。
  `water-01`（標準差 5.3）與 `ore-01`（4.6）不在本次清單內，仍是舊版純色。

- [2026-08-24] **地面裝飾散佈物 12 張**（批次 3）。石頭 ×3、灌木 ×3、野花 ×2、樹樁、
  倒木、小水窪、車轍，成品 7–21px、各 ≤32 色，無底座可疊在任何地形上。
  新增 `scripts\process-decor-sprites.mjs`（建築類管線：floodfill 去背 → trim →
  Lanczos 按**高度**縮 → pngquant 32；按高度而非寬度縮，因為小物件的辨識度取決於高度剪影）。
  **這批的技術關鍵是降採樣倍率**：成品只有 8–20px 而 raw 是 1536px，倍率 50–100 倍，
  所以生圖 prompt 必須要求「極簡剪影 ＋ 極粗描邊（raw 上 40–60px）」——
  1px 描邊在這個倍率下會直接消失。實測 12 張全部可辨識。
  已用決定論 hash 散佈到 5×5 草地做用途模擬：空曠感確實消除。
  **偏弱項**：車轍太小太淡（8×7），在草地上幾乎看不見，日後可放大並加對比。

- [2026-08-24] **素材債清理與建築變體 7 張**（批次 5）。
  `house-01` 重生（舊版自帶一整圈草地方形基座，違反現行無底座規則）、
  `farm-01` 重生（舊版沿菱形外緣描黑邊，縮到 64×32 後斷成 34 個散落黑點，像髒污——
  新版明確要求「只有木柵欄與作物描邊，田地外緣不描邊」）、
  新增外觀變體 `house-02`（茅草頂）／`house-03`（石牆）／`farm-02`（金黃麥），
  以及建造中用的 `scaffold-01`（外框式鷹架，中間可看穿）。
  `tile-forest-01` 走到 v5：v2 的樹冠在實機大視野下仍成陣列，v5 改為「連續林冠層、
  樹冠互相咬合、看不出單棵分界」，鋪排後看不出格界。
  新增 `scripts\process-building-sprites.mjs`（建築管線：按**寬度** N×64 縮，
  並檢查 art-bible 的高度上限）。六張建築成品 64 寬、高 44–72，全部落在 ×1.5 上限內。

- [2026-08-24] **M5-W1 看得見的城市：UI 元件層與海岸線過渡**（691 tests，+11）。
  兩條並行、檔案不相交。
  **W1a 美術探路**：Codex `$imagegen` 生成 8 張單邊過渡 tile（水面浪花 ×4 方位、
  沙灘草緣 ×4 方位）與缺失的 `market-01`。新增 `src/render/terrainTiles.ts` 純函數
  `terrainTextureKeyFor`：由「本格地形 ＋ 四正交鄰格」決定 texture key，多邊臨界時取固定
  優先序（tl→tr→br→bl）——**渲染必須是 state 的純函數，否則分塊重烘會閃爍**。
  `TerrainRenderer.bake()` 改用它，並新增 `readTerrainPatch` 把 chunk 外擴一圈的地形
  一次預讀（否則 terrainAt 呼叫從每格 1 次變 5 次，而它是烘焙成本的大頭；預讀後只多約 6%）。
  `invalidateTile` 改為連四個鄰格所屬 chunk 一併標髒——過渡讓選圖依賴鄰格，只標自己
  會在 chunk 交界留下不更新的舊接縫。
  **修掉現成 bug**：`market` 一直沒有素材，`CityScene` warn 一次後永久略過，
  市場在畫面上是隱形的；選單第 10 格也是空白縮圖。
  **W1b UI 元件層**：新增 `src/render/ui/theme.ts`（色票/字體/框線 token，零 Phaser 依賴）
  與 `ui/draw.ts`（`drawFramedRect`、`uiTextStyle`）。此前同一個黃銅色以七個名字散在四個檔，
  各檔註解得互相寫「與某某同色」才說得清；`fontFamily/stroke/strokeThickness` 三行在六處
  逐字重複。五個檔改為引用 token，**測試零修改全綠**（純重構，行為不變）。
  以三個突變（海岸判斷反轉、方位偏移改錯、沙草交界誤認 forest）驗證新測試非盲測。
  工具面：`npm run screenshot` 新增 `--center gx,gy`（把鏡頭移到開局視野外驗證渲染）；
  `scripts/process-terrain-tiles.mjs` 的 pngquant 路徑加 fallback（原本寫死相對路徑，
  在 git worktree 下會落空）。

- [2026-08-24] **M4.6 實玩前置：存檔接線與封死防護**（680 tests）。實玩驗收前先補掉兩個
  會讓回饋失真的洞。
  - **存檔終於接上遊戲**：`src/core/save/save.ts` 有 440 行、三個測試檔、五個 schema 版本、
    還過了 Codex 第二審，但 render 層對它的引用是**零**——玩家一次都存不了，重新整理就
    整座城市歸零。新增 `src/render/persistence.ts` 把它接到 localStorage：
    **每跨一個遊戲日自動存檔、開機自動續上次進度**。做成自動而非按鍵讀檔，是因為真正的痛點
    是「重新整理就沒了」，而會忘記按鍵的情境正好就是會弄丟進度的情境。
    存檔失敗（配額用盡等）會在 HUD 明說；壞檔不自動刪除（玩家可能想搶救），改為開新局並提示。
    `N` 鍵開新局，比照拆除民居的兩段確認。
    端到端驗證：跑過日界 → 真的 reload → tick 709 進、709 出，存檔 1258 bytes，無 page error。
  - **不准把建築封死**：格子被建築佔住即不可通行，四面蓋滿就會讓居民永遠走不出來、
    工人永遠到不了，而畫面上毫無徵兆。`canBuildAt` 新增判定並在放置預覽直接擋下。
    判準是「**這次放置造成的變化**」而非「放置後是否封死」——本來就被地形圍住的建築
    （水中央的一格草地）在此規則之前就蓋得起來，不該追溯變成非法。
    只檢查正交四鄰而非完整連通性：完整連通要掃整張地圖，成本與實際要防的情境不成比例。
    突變驗證：拿掉檢查 3 條轉紅；改成「放置後是否封死」則地形孤島那條轉紅。

- [2026-08-24] **平衡框架建址缺陷修正——先前回報的四個「平衡問題」有三個是量測假象**。
  照建議進入平衡定值前先驗證工具本身，結果 `balance.ts` 的腳本玩家有兩個缺陷，
  讓 30 天曲線量到的根本不是遊戲的數值問題：
  - **建址錨點釘死在世界中心**，城市長大後新建築離住宅越來越遠。實測三座採石場距住宅
    23.7／24.7／33.7 格，而通勤上限是 24——兩座永遠沒人到得了，產能恆為 0，
    建造成本卻照樣扣掉。改為以住宅重心為錨點後，通勤距離自然落在範圍內。
  - **緊密螺旋擺放會把居民封死在家裡**：修好錨點後才浮現，有居民的家四個正交鄰格全被建築佔滿，
    他永遠走不出那一格，被指派的磨坊到崗數恆為 0，全城慢性餓死。
    改為要求新建築不與任何既有建築正交相鄰，保證每棟四周都留有可走的空格。
  - 另修策略的兩個連帶問題：食物鏈條數要跟人口成長（原本 1/1/1 就算「平衡」而永不擴充），
    以及所有 jobs>0 的建築都要有閒置勞力才准開工——jobs system 先搶先贏，
    多開養不起的職缺等於把勞力從既有產線稀釋掉。
  **修正後的曲線**：人口 6→36、建築 6→27、糧食 15060、石材 8325、鐵礦 2510、鐵錠 240、金幣 1165。
  先前回報的「石材第 27 天歸零」「金幣卡在 70–93」「金屬鏈完全沒被蓋起來」**三項全部消失**，
  它們量到的是勞力到不了，不是資料表失衡。護欄基準已重訂，並加上
  「石材與鐵礦 30 天內必須真的產出」的回歸鎖（突變驗證：拿掉連通性保護會轉紅）。

- [2026-08-23] **M4.5 存檔第二審修正批**（663 tests）。依專案規則對存檔 schema 變更做 Codex
  第二審，提出三項；主對話逐項實測後採納兩項：
  - **超出一批的 progress 會每 tick 重複出貨**（已修）：實測存檔帶 progress=1000、workTicks=10
    時，5 個 tick 就印出 5 批。手改存檔或「資料表把 workTicks 調小後載入舊檔」都會造成。
    改為讀取時把 progress 夾在 [0, workTicks]。
  - **地形採乾與原料斷炊同時發生時，已挖出的資源永遠卡在進度裡**（已修）：舊的結算條件
    附帶 `jobRatio > 0 && inputRatio > 0`，但那兩者為 0 時同樣採不到地形。改為只看
    「地形有沒有被擋住」，直接以 findTerrainSource 回 null 判定。
  - 多產出地形建築的各資源比例（**未採納**）：實測 20 單位地形供給下產出總和恰為 20，
    守恆成立，未能重現。此組合本就登記在「規格缺口」且無實際建築使用，維持登記狀態。

- [2026-08-23] **M4.5-W4 自動化平衡框架**（Codex 實作，主對話驗證）。新增 `src/tools/balance.ts`
  與 `npm run balance`：一場「有玩家在玩」的 headless 模擬。既有的 `npm run simulate` 只能跑
  靜止城市——建築開頭一次放好、之後沒人動手，所以曲線看到的資源單向暴衝不是平衡壞掉，
  是沒有需求端。玩家策略補上這塊：住房將滿就蓋房、糧食低於門檻就補食物鏈最短的一環、
  有餘裕才擴充其他鏈，座標一律經 `canBuildAt`。
  實跑 30 天：建築 6→44、人口 6→36、住房容量隨人口成長——這才是能拿來調數值的曲線。
  `tests/tools/balance.test.ts` 的目標帶是**描述現況的護欄而非設計目標**，用途是資料表被
  改動時當場紅。主對話突變驗證：農場產量 4→8、稅率 2→10 各自都讓護欄轉紅。

- [2026-08-23] **M4.5-W3 建築進度條**：宣告 workTicks 的建築在頭頂顯示進度條，走滿即交貨。
  條同時是效率指示器——走得慢就代表在崗人力不足或原料短缺，補上 W1 留下的
  「玩家看不到單棟建築實際效率」缺口。不分批與無產出的建築（民居、市場）不畫條：
  對它們而言「一批」不存在，畫一條永遠停著的條只會誤導。
  只在 sim tick 後重畫、且只畫視野內建築。版面算式抽成 `progressBar.ts` 純函式。

- [2026-08-23] **M4.5-W2 整批產出，存檔升 v5**。宣告 `workTicks` 的建築每 tick 照常按
  jobRatio/inputRatio/地形實產算出「這一 tick 做了多少」，但累積成工時進度而非直接進資源池，
  滿一批才一次入帳。原料與地形仍逐 tick 扣，所以中斷不浪費、也不會「做完才發現沒料」；
  平均產率與分批前完全相同，改變的只有顆粒度與可見性。
  **省略 workTicks ＝ 不分批**——一批多長是各建築的設計數值，依資料驅動鐵則寫在資料表，
  程式端不預設「大家都分批」的常數。
  **地形採乾的結算**：有人在崗、原料也夠卻產不出來時，把未完成的進度按比例結算並歸零。
  樹已經砍掉了，留在進度裡等湊滿一批會讓玩家看到「最後幾棵樹憑空消失」。
  **開局糧食 100→1200**：三段食物鏈各要滿一批才交貨，開局約一整個遊戲日才有第一批麵包，
  舊值讓六名居民在第一個日界集體餓死——而當時 640 條測試全綠。穩態每日淨增約 300，
  純粹是產線啟動空窗。新增 `tests/render/demoWorld-survival.test.ts` 當開局存活的回歸鎖。

- [2026-08-23] **M4.5-W1 到崗才算數**（629 tests）。`production.ts` 的在職人數改為只計入
  「站在建築 footprint 上」的居民；先前只數 `citizen.job === building.id`，居民還在路上
  建築就已滿產能運轉，走路動畫純屬裝飾、通勤距離完全不影響經濟。座標用精確相等比較，
  與 movement 判斷「已在目標格」用同一條件，兩邊不會對同一居民有不同看法。
  **實測效果**：demo 世界 20 天糧食 32392→5472（日淨增 1600→244），木材 14900→7700，
  平均到崗率 3.6/8。原本的資源暴衝大致收斂。
  **意外的湧現**：穀物開始以每日 +80 累積——農場離住宅 2 格（有效工時 280/300），
  磨坊離 4 格（260/300），上游比下游更有效率就會漏出中間產物。佈局遠近開始真的影響經濟，
  這正是這個改動要的效果。
  既有 10 條測試轉紅全部是「工人放在 (0,0) 但建築在他處」的 fixture，逐一改為把工人放到
  建築格上（保留原意，非放寬斷言）；其中「新居民出生後隨即產出」那條的前提真的變了，
  改寫為「指派後仍要走到工作地才算在職」。新增 12 條專測到崗判定——既有 production 測試
  把建築與居民都放 (0,0)，對這個行為全盲。突變驗證：拿掉到崗判定會讓 7 條轉紅。

- [2026-08-23] **M4-W3 MVP 收尾：可玩性介面與通勤上限**（617 tests）。
  **建築選單**改為一排縮圖格子（`src/render/BuildingPalette.ts`）：每格畫出該建築的實際素材與
  快捷鍵數字，買不起的壓暗並把邊框標紅。舊版只有一行「按 1-9/0 選擇建築」，玩家得把 11 棟
  建築與按鍵的對應全背下來——那不是簡潔，是把資訊藏起來。
  **市場面板**（按 M 開關）補上 W2 缺的操作管道：列出可交易資源的庫存、賣出得多少、買入付多少，
  點鈕即下 trade 指令；沒蓋市場時開不起來，與 core 的 trade 判定同一條規則。
  **遊戲速度**空白鍵暫停、`-`/`=` 調 1×/2×/4×；倍率乘在累積的實時毫秒上，固定時步不變，決定論不受影響。
  **拆除確認**：拆有住戶的民居要按兩次右鍵，第一次只出警告。只攔住戶不攔工人——工人下一 tick
  會被重新指派，是可回復的；全都攔會讓玩家養成無腦連按兩次的習慣，這道關卡就白設了。
  **通勤半徑硬上限 24 格**（Codex 實作）：居民速度 0.1 格/tick、半日 300 tick，理論極限 30 格，
  留 20% 給繞路。超限的工作不再指派，杜絕「居民空轉且該工作永遠沒人做」。
  版面計算全部抽成純函式（paletteLayout/marketLayout/gameSpeed/demolition）以便單元測試。
  突變驗證：拿掉通勤上限檢查會讓兩條測試轉紅（主對話親自跑過，非轉述 Codex 回報）。

- [2026-08-23] **M4-W2 經濟：稅收與市場**（567 tests）。新增 `data/economy.json`（稅率、市場加價率）、
  資源表加 `basePrice`（gold 無價＝不可交易）、建築表加 `market` 與 `enablesTrade` 旗標。
  稅收 system 於日界依**就業**人數課稅——綁在就業上，收入才反映城市真的在運轉，
  否則玩家只要蓋房增加人口就能穩定生金、生產鏈的安排與收入脫鉤。
  市場交易走新的 `trade` 指令（買/賣、正整數量、上限保護），必須先蓋出 `enablesTrade` 建築才解鎖；
  買價 = basePrice×1.5、賣價 = basePrice，價差讓「賣掉再買回」必定虧損。
  庫存或金幣不足一律整筆跳過、不做部分成交（沿用 placeBuilding 的不部分扣款慣例）。
  交易操作 UI 併入 W3 的建築選擇 UI 一起做，W2 只落地核心與測試。
  新增 42 條測試，並以突變測試驗證非盲測（拿掉「只課就業者」與「必須有市場」兩道檢查各自轉紅）。

- [2026-08-23] **M4-W3 建築素材批（提前完成）**：磨坊/麵包坊/鋸木廠/礦坑/冶煉廠/鐵匠鋪六張素材，
  Codex `$imagegen` 生成後以 ImageMagick 白底 floodfill 去背→trim→Lanczos 縮至寬 64→pngquant 32 色。
  **降採樣改用 Lanczos 而非 point**：12 倍降採樣下 point 會打散風車葉片與梯子這類 1px 細長結構
  （目視比對確認），Lanczos 保住結構且與既有 house-01 等素材的平滑度一致。
  同批把建築選單改為分頁（每頁 10 棟，`[` `]` 換頁），並用測試鎖住「每一棟建築都選得到」。

- [2026-08-23] **M4-W1 生產鏈與加工鏈完成**：資源表擴充至 10 種（grain/flour/plank/iron-ore/iron/tools），
  建築表擴充至 10 棟（新增 mill/bakery/sawmill/mine/smelter/blacksmith），`BuildingDef` 新增選填
  `inputs` 欄位並由 loader 嚴格驗證（值須為正數，異於 cost/production 允許 0）。
  生產系統改為**比例分配**：先按全體消費者名目需求算出分配係數，再依 jobRatio、inputRatio 與
  地形實產比例扣料，因此結果與建築處理順序無關。單審 9 條全修，其中三條是「遊戲跑不起來」級：
  開局建築全改後產不出食物（人口第 3 天歸零且無法翻身）、鐵匠鋪因只綁 1-9 鍵而永遠選不到、
  鐵匠鋪造價 135 金幣但開局只有 100 且無金幣來源（開局金幣提高到 150）。
  審查另**推翻我對比例分配的疑慮**：實測貪婪分配依陣列順序給出 0 或 150.5 tools，
  比例分配恆為 198.67——下游吃多種原料時，貪婪把某一項歸零會讓終端產出直接變 0。
  快轉工具的就業優先序改為由 inputs 反向推導食物鏈深度（上游優先），不再依賴「誰產 food」。
  新增 40 條測試（production-inputs、production-chain），封住多產出＋地形部分供給的「只扣不產」缺口。

- [2026-08-22] **M3.9-W4 完成，M3.9 里程碑收尾**：地形素材批六型（水/沙灘/森林/岩石/礦脈/高山，
  無縫驗證 24803 像素零破洞）＋分塊 RenderTexture 渲染整合（chunkSize 64、視野內才烘、
  LRU 淘汰上限 12 塊、override 快照比對觸發重烘）＋小地圖（一格一像素＋視野框）＋
  起始區域決定論選址（Chebyshev 環由內而外找能放下全部起始建築的 anchor）＋世界尺寸統一
  （移除 render 自己的 GRID_SIZE=12 與 demo 地形 fixture，一律以 state.worldSize 為準）。
  **渲染效能實測（主迴圈補做，Codex 因瀏覽器權限未能完成）**：1000×1000 初始化到可視 1157ms
  ——比 200×200 的 1216ms 還快，場景物件數固定 16~19 個，證實成本與世界大小完全脫鉤；
  平移 FPS 在 1280×720 為 58.6、960×540 為 58.4，2560×1440 降至 19 —— 經同世界不同視窗
  對照確認為**軟體算繪的填充率瓶頸**（像素 4 倍、FPS 約 1/3），與世界大小及邏輯無關。

- [2026-08-22] **M3.9-W3 完成**：大地圖適配。`distanceField` 加入節點預算（省略即無界、向後相容，
  審查以 400 張隨機地圖 92497 次查詢比對舊實作 mismatch 0）；movement 改為有預算上限的最佳努力
  搜尋；jobs 依 home 的 Manhattan 距離指派最近空缺。
  **效能實測：200×200／40 居民／300 tick 從 7834ms 降到 4ms；1000×1000／50 居民／100 tick 為 3ms**
  ——百萬格世界在模擬端證實可行（配合 W1 的地形隨用隨算與 T2 的分塊渲染方案，三根支柱齊備）。
  單審三裁決項兩過一不過：快取未破壞無狀態不變式（以「同實例連跑 400 vs 兩實例各跑 200 深等」證明）、
  嚴格下降在四連通格上數學排除所有循環（3000 情境零循環）；**F1 不通過——貪婪退化遇任何凸出障礙
  即永久凍結**（一棟 1×1 建築擋在直線正中就夠，實測 20000 tick 不動、真實地圖 4-6% 居民凍結），
  根因是**主迴圈規格過寬**（測試明文允許「卡死不動」為合法結果，故永遠不會紅）。
  修法改為有界最佳努力搜尋並**收緊測試**（移除卡死放寬、鎖定單障礙必須繞過抵達）。
  修正後實測：凍結比例 4-6% → **0.0%**，繞路成本正好 +2 步（10 格通勤第 120 tick 抵達、
  20 格第 220 tick 抵達）。終態 486 tests 綠。

- [2026-08-22] **M3.9-W2 完成**：地形綁定生產與資源耗竭再生。伐木場需鄰接森林並消耗之、
  採石場需蓋在石礦上、農田要草地、民居草地或沙灘；`canBuildAt` 成為 placeBuilding 第四關；
  砍光的森林轉草地並記 `depletedDay`，5 遊戲日後回復，礦脈不再生；新增 `regrowth` system。
  單審 12 條全修，其中 **F1 是主迴圈的規格錯誤**——原規格把「地形作為擺放條件」與「地形作為
  被消耗的資源」混為一談，導致農田（on: grass，而草地容量 0）永遠產 0 糧、demo 世界 8 日內
  全員餓死，而 449 個測試對此完全無感（審查以反向突變證實翻轉行為仍全綠）。修法為拆出獨立的
  `terrain.consumes` 欄位並補真實資料表測試。另修：F2 regrowth 從未接線（死碼）、F3 平衡
  （森林 60→2400、石礦 120→4800，一格約撐 2 遊戲日）、F4 改為真正資料驅動（原本容量硬寫在
  core，改 JSON 無效）、F5 擺放預覽接上 canBuildAt、F8 CLI 的 full-sim 改用決定論的地形感知
  落點（原本建築全落在海上，曲線無意義）。終態 459 tests 綠。
  實測自證：農田在草地 10 tick 產 30 糧且不消耗地形；森林格第 1200 tick 耗盡＝正好 2 遊戲日。

- [2026-08-22] **M3.9-W1 完成**：地形核心（hash noise 程序生成、島嶼遮罩、terrainAt 查詢、
  terrainOverrides 差異表）＋存檔 v4（三欄位＋from:3 遷移）。雙審 12 條全修：**C1 地形演算法
  版本欄位**（防日後改生成常數使舊檔地圖變形而無錯誤訊息）、C2 遷移時為舊檔建築/居民腳下建
  grass override、C3 override 鍵格式與越界驗證、C4 seed uint32 正規化、F1 序列化端守門、
  F3 小數座標 fail fast、F4 own-property 測試改為原型污染式（原測試恆真）、F5 worldSize 下限 10
  （代數保證在 size<5 崩潰）、F6 生成器指紋測試（原性質門檻擋不住常數突變）。終態 379 tests 綠。
  **T2 渲染方案定案：分塊 RenderTexture、視野內才烘**（見 `.pipeline/reviews/2026-08-22-spike-
  terrain-render.md`）——1000×1000 下 heap ~30MB 且初始化與世界大小解耦；Phaser 內建 isometric
  Tilemap 遭否決（heap 256MB、cull 每幀掃全圖（原始碼確認）、且有未解的格間縫隙缺陷）。

- [2026-08-22] **M3.5 視覺打磨波完成**（使用者實玩回報後插入）：視窗改 `Scale.RESIZE` 填滿
  （2K 全螢幕不再是小方框＋黑框，HUD 橫貫上下緣，另修 Phaser 邊界夾制把地圖釘左上角的問題）；
  村民走進建築格即隱藏＝「進屋工作」（探針 220 筆取樣零違反）。單審三個裁決項全通過
  （攝影機邊界改動 21 組數值比對等價、dev 全域 production 零命中、隱藏時機不誤判第三方建築），
  找到並修掉 F1 潛伏 bug（場景重啟後攝影機失去邊界——改為無狀態化根治）＋F2-F5；
  另抽出 `cameraBounds`/`hudLayout` 純函式補 17 條單元測試。終態 309 tests 綠。

- [2026-08-22] **M3 完成**（三波）：W1 居民資料模型＋存檔 v3（Citizen、v2→v3 遷移、housing/jobs
  欄位、population.json；T1 例外 Codex 雙審，F1-F3 修畢）；W2 人口/職業/在職率生產＋A* 尋路
  ＋movement（雙審 12 條：movement 經裁決重工為無狀態純函數步進模型——穿牆/存檔不一致/抖動
  全滅，距離場 BFS 順帶把 40×40 效能從 407ms 壓到 0.165ms）；W3 村民渲染（Codex 生圖 2 款）
  ＋HUD 人口＋快轉 --full-sim 人口曲線（單審 13 條，F1-F9/F11 修畢，平衡三候選實測後定案 C）。

- [2026-08-22] **M2-W2 完成**：core 建築指令（placeBuilding/removeBuilding＋佔格＋成本，TDD）、
  擺放 UI＋HUD＋固定時步遊戲迴圈（雙攝影機、sprite diff）、Playwright 截圖工具（npm run screenshot）。
  雙審（Claude 全波＋Codex 限 T2 存檔面）合併 12 項全修：指令數值/字串邊界、**id 帶 #tick 防 ABA**、
  **SAVE_SCHEMA_VERSION 升 v2＋no-op 遷移**（清掉 v1 端到端遷移測試債）、CLI defs 注入、
  HUD 誤觸防護、size 上限。終態 177 tests 綠。
- [2026-08-21] **M2-W1 完成**：素材批（等距地形 tile×3＋伐木場/採石場，Codex 生圖）、
  等距座標模組 iso.ts（TDD）、Phaser 3.90 接線（Boot/City 場景、攝影機、修復 lock 檔既有損壞）。
  單審 16 條全修；調色盤經 remap 實驗裁決放寬（DB32 為指引非硬約束）；
  素材管線 v3（tile 用 fuzz 白背去除修邊緣光暈）。

- [2026-08-21] **M1-W2 完成**：資料驅動層（ResourceDef/BuildingDef、loader 嚴格驗證、
  data 兩表、production system）＋存檔 v1（SAVE_SCHEMA_VERSION、pendingCommands 入 state、
  serialize/deserialize 逐欄驗證、applyMigrations＋gap 偵測）＋快轉 CLI
  （`npm run simulate`，--buildings 情境、CSV 曲線；Codex gpt-5.6-sol 實作）。
  單審（依使用者指示不派 Codex 第二審）出 13 條全修；複審 13/13 落實（突變驗證），
  殘留 R2-1/2/3/5/8 修畢、R2-9 主迴圈實測誤報。終態 128 tests 綠。

- [2026-08-21] **M1-W1 完成**：Vite+TS+Vitest scaffold；決定論模擬核心（mulberry32 RNG、
  時間系統 600 tick/日×30 日/季×四季、GameState、System 介面、指令佇列、固定時步 Simulation）。
  TDD（紅綠證據＋manifest）；雙審（reviewer 突變測試＋Codex 探針）出 14 條裁決，1-11 全修，
  複審 16 突變全殺再出 N 系列，N1-N5/N8-N11 全修；終態 50 tests 綠、tsc 0 錯。
  證據：`.pipeline/reviews/`（3 檔）與 `.pipeline/tdd/`。

- [2026-08-21] **M0 完成**：art-bible 經 5 張實圖核可（DB32/等距 64×32/左上光源/1px 描邊/
  無底座規則）；工具鏈裝齊（rembg 2.0.76、ImageMagick 7.1.2、pngquant 2.17.0）；
  Codex $imagegen → 搬運 → rembg → 點採樣 → pngquant 全管線驗證通過（5 張 32 色達標）；
  定調參考圖存 `assets\refs\`；Codex trusted 已設定。
- [2026-08-21] 整體架構計劃核可（`~\.claude\plans\misty-wondering-peacock.md`）：
  三層疊加設計（中世紀經濟／威脅層／奇幻層）、M0–M9 里程碑、Claude+Codex 分工。
- [2026-08-21] 目錄骨架、git init、三文件（README/CLAUDE/PROGRESS）、.gitignore/.gitattributes（全 LF）。

## 進行中

（無——M2 已收尾，待使用者實玩驗收）

## 待辦

- **【下一個 session 從這裡開始】AoE2 美術五波（2026-08-25 使用者定案，優先於 M5-W2）**。
  北極星＝世紀帝國 II／Stronghold 的 2D 預算圖感：暗橄欖色調、高雜色密度、寫實偏暗、
  地形有斜坡、建築濃細節。在**現有 tile 架構內**執行，不換渲染路線。
  生圖走 codex-imagegen＋三支後處理腳本，鋪排驗收跑 `verify-terrain-tiles.mjs`（判準見
  art-bible「地形 tile 的驗收」——禁方向性紋理、禁高對比地標、5×5 目視不可省）。

  - ~~W1 定調重生~~ 已完成（2026-08-25，見已完成區；使用者色調確認中）。
  - **W2 變體混鋪＋裝飾散佈**：`terrainTextureKeyFor` 加決定論變體選擇（同地形依格座標
    hash 挑 grass-01/02/03，素材已登錄在 assets.ts）；新增 DecorRenderer 把 12 張
    `decor-*` 裝飾物決定論散佈到草地/沙地（視野裁切；密度可調；車轍太淡可趁機重生）。
    驗收：截圖無壁紙感、`npm run screenshot` 效能無感退化。
  - **W3 斜坡**：階地裙邊從「垂直土壁」改為 AoE2 式斜坡面——生斜坡 tile（四方位，
    比照過渡 tile 的 tl/tr/br/bl 命名與選圖機制）；`ELEVATION_STEP` 視效果調整（8→10px?）。
    驗收：海岸與丘陵交界截圖、`verify-terrain-seams` 通過。
  - **W4 大建築與建築濃度**：render 支援 2×2 建築（`placement.ts` 的 w/h 參數已有雛形、
    `buildingSize` 已存在）；tavern（128 寬素材已在）接入；主要建築以 AoE2 濃度重生 1-2 輪
    （更多細節、更深色調），走 `{raw,out}` 映射落到現用 key。驗收：實機截圖對比。
  - **W5 色調統一收尾**：全畫面色調微調（暗角/飽和度，可與 M5-W4 的日夜濾鏡同一個
    shader 接入點一起做）；全套「改造前 vs 後」對比圖交使用者驗收。
  - 註：批次 2b（山體疊加 sprite）視 W3 斜坡完成後的畫面再議；
    `water-01`／`ore-01` 仍是舊版純色，可併入 W1 或 W2 順手重生。

- **M4.5 生產模型重做（插在 M5 之前，2026-08-23 使用者裁決走「到崗才算數＋建築進度條」）**：
  - ~~W1 到崗才算數~~ 已完成（見上）。
  - ~~W2 建築進度條（模擬側）~~ 已完成。**inputs 的裁決：兩者都不是，改為「每 tick 照常
    按比例扣料、產出累積成進度」**——既有的比例分配數學完全不動，同時避開「中斷浪費原料」
    與「做完才發現沒料」兩個坑，平均產率也不變。
  - ~~W3 進度條呈現~~ 已完成。數值重定改為等實玩回饋再做（見下方平衡登記）。
  - ~~W4 自動化平衡框架~~ 已完成（腳本化玩家策略＋目標帶斷言）。**常數網格搜尋尚未做**——
    有了會成長的曲線與護欄之後才有意義，但要先由使用者定出「什麼叫平衡」的目標帶，
    自動調參只能滿足設定的目標，判斷不了好不好玩。
  - **速度 vs 半徑的裁決結果（W1 實測後）：兩者都不動**。有效工時 = max(0, 300 - 10×通勤距離)，
    24 格處剩 10% 效率。但這不需要額外限制——jobs 本來就優先指派最近的空缺，
    只有近處全滿才會派到遠處，而 10% 效率的工人仍勝過閒置。距離的懲罰由效率自然表達，
    再加一道人為上限只會讓玩家看不懂為什麼不能派人。現有 24 格硬上限的原意是擋掉
    「永遠走不到」（>30 格）的情況，那個目的仍然成立，維持不變。
  - ~~[W1 登記] 玩家看不到單棟建築的實際效率~~ 已由 W3 的進度條解決。
  - [W3 登記，外觀] 進度條全部畫在同一個 depth，等距下後排建築的條會蓋在前排建築身上。
    要正確排序得替每棟建築各開一個繪圖物件，成本不划算；目前條很細、影響輕微，先接受。
- M5 塔防；M6 冒險者公會；M7 魔法；M8 種族；M9 上架。
- ~~自動化平衡測試的三塊拼圖~~ 玩家行為模型與目標帶斷言已於 M4.5-W4 完成；
  只剩調參搜尋未做，且必須等使用者定出目標帶才有意義（見 M4.5 待辦）。
- **[2026-08-24 重測，待實玩後定案] 修掉工具缺陷後仍然存在的兩個平衡現象**
  （`npm run balance` 可重跑）：
  1. **穀物累積到 8650**：農場 4 座、磨坊 4 座棟數相同，但農場離住宅較近、有效工時較長，
     上游產得比下游吃得快，差額逐日放大。這是「到崗才算數」帶來的佈局效應，方向正確但幅度偏大，
     可考慮調整食物鏈各段的 jobs 或 workTicks。
  2. **工具 30 天內仍為 0**：鐵匠鋪要吃 iron＋plank，而 30 天時鐵錠才剛累積到 240。
     金屬鏈已經在跑（礦坑→冶煉廠），只是還沒走到終點。若期望玩家在 30 天內做出工具，
     前段成本或工時要放寬。
  這兩點都需要你實玩後回報手感才好定值。
- ~~玩家可能把居民封死在家裡~~ 已於 M4.6 處理：直接在 `canBuildAt` 擋下會造成封死的放置。
  選「擋下」而非「警告」或「引入道路」的理由：可略過的警告仍會讓玩家在不知情下弄壞城市，
  而道路是會改變整個遊戲感覺的大系統；擋下是移除這個隱形不可逆失敗的最小改動。
  若日後想要更寬鬆（例如允許刻意圍城），這條規則集中在一個函式裡，好改。
- [登記 M4-W3] 市場交易固定每次 10 單位，沒有數量輸入。若實玩覺得點太多次，
  再考慮 Shift 點擊 ×10 或加數量欄。
- [審查登記 M3-W3] 2×2 建築落地時：工人在建築格內的 depth 遮擋語義（F10）；職業指派依陣列序（F13）。
  （F13 的「拆民居靜默蒸發住戶」已於 M4-W3 以兩段確認解決。）
- [審查登記 M3-W3] expandBuildingSpecs 容量上限改 grid×grid（預設 144）且 --grid 綁 --full-sim：
  非 full-sim 大量建築掃描（>144 棟）目前做不到，需要時再放寬。
- ~~快轉 CLI 的 `--full-sim` 建築不經地形檢查~~——查證已由 M3.9-W3 的 `expandBuildingSpecs`
  解決（帶 state 時以 `canBuildAt` 由中心向外環狀搜尋可建格），W3 實跑確認農場能正常產穀物。
- [登記 M3.9-W2] 規格缺口：一棟建築同時有地形綁定又產出多種資源時，地形資源池如何在多個
  產出項間分配未定義（目前所有帶 terrain 的建築都只產單一資源）。M4 加工鏈出現複合產出時補。
- [登記 M3.9-W2] `src\render\demoWorld.ts` 的 `applyDemoTerrain` 是暫時 fixture：demo 區 12×12
  落在 200×200 世界角落、core 判定為 water，故先覆寫成草地＋森林＋石礦讓產出鏈可跑。
  **W4 讓 render 直接讀 core 地形後整段刪除**。
- [登記 M3.9-W2 審查] F6 多格建築的 near 偏移只套在 footprint 原點（2×2 伐木場可靠腳下那格
  通過檢查並砍掉身下地形）；F9 `isAreaFree` 被執行兩次；F10 applyCommand 直呼路徑帶非整數座標
  會 throw 而非靜默跳過；F11 耗盡的 rock override 永不移除，長局可能撞上 200000 上限導致
  「能玩但存不了檔」；F12 每 tick 每建築重複呼叫 terrainAt 可省一半。
- [登記 M3.9-W2 審查] 建議凍結一份「911c638 實際寫出的 v4 存檔字串」當 fixture——現有舊檔
  載入測試是拿當前 state 物件序列化而來，證明的是「當前 shape 少一欄可載入」，不是真實舊檔。
- [審查登記 M2-W1，補登] 地形渲染擴張路徑：現行每格一個 Phaser Image（12×12＝144 個），
  地圖 >64×64 時必須改 Tilemap 或烘成 RenderTexture／Blitter，否則物件數爆炸。**M3.9-W1 處理**。
- [審查登記 M3.5] 三項存疑/延後：(F6) `scene.remove()` 直接銷毀不 emit SHUTDOWN，resize 監聽
  理論上殘留（restart 路徑實測無洩漏，監聽數恆 5）；(F7) 日夜目標翻轉時折返中的居民最壞
  連續 0.5 秒被誤判為在建築內而隱藏（純美觀）；(F8) 多格建築的隱藏判定點是 origin 格
  ＝等距後上角，2×2 落地後村民會在建築背面消失——與 F10 的 depth 語義一起處理。
- [審查登記 M3.5] HUD 字級縮放用 monospace 字寬估算（0.62×字級）而非 Phaser 實際量測，
  極窄視窗（<200px）理論上仍可能小幅溢出；若之後 HUD 換字體需重校此係數。
- [缺口 2026-08-22] **地形無資料模型**：`terrainTextureAt(gx,gy)` 是 render 端純函數，
  地形不在 GameState、不進存檔；且生產與地點完全無關（伐木場不需要森林）。
  大地圖／自然資源／地形綁定生產都卡在這裡——見下方「重要決策紀錄」的地圖規模討論。
- [審查登記，M4.6 部分處理] 存檔已於 M4.6 接上（自動存/自動載入）。**仍未做**：載入後對
  buildings type 做進場全量檢查（未知 type 目前於首 tick 才爆），以及統一 occupancy（1×1 fallback）
  與 production（throw）對未知 type 的策略。資料表刪掉某個建築 id 時舊存檔就會踩到。
- [審查登記] pending 指令語意隨資料表版本漂移屬資料驅動設計限制（存檔綁資料表版本）；未來 schema
  升版任務評估是否對指令快照成本。遷移撰寫紀律：每次升版同步擴充 deserialize 欄位驗證。
- [審查登記] fastforward main 接線無自動化測試（I/O 皮層排除）；改動 main 參數傳遞時手動冒煙。
- [審查登記] uiCamera 尺寸取 create 當下值：改用 RESIZE/FIT scale 模式時需同步處理 HUD 裁切。
- [審查登記 M3] A* 效能：popLowest O(n) 掃描＋日界全城同步重算，40×40/150 人實測 407ms/tick——
  擴圖前需 binary heap＋錯峰/分幀；jobs 無空缺仍每 tick 全掃屬同類（F6/F11）。
- [審查登記 M3] 負糧食時日界消耗反向補正至 0（現無觸發路徑，F9）；在職率使資源出現小數，
  日後 cost 可負擔判定與糧食比較需注意浮點（F10 已修顯示層）。
- ~~SAVE_SCHEMA_VERSION 升 v2 必補端到端遷移測試~~——已於 M2-W2 修正批完成（v1 存檔經 no-op 遷移
  載入＋無遷移 throw＋續跑測試均已落地）。

## 已知問題

（無）

已排除的疑似問題：
- [2026-08-23 結案] 「隱藏分頁載入開機即崩」（canvas 0×0＋`Framebuffer status: Incomplete
  Attachment`）**是假性 bug，僅限 Claude 內建瀏覽器**（該環境 `document.hidden` 恆為 true）。
  使用者實測一般 Chrome 開啟正常，headless chromium 亦正常。不需修改 boot 流程。
  日後若在內建瀏覽器再看到此錯誤，直接視為環境限制，改用 `npm run screenshot` 取證。

## 重要決策紀錄

- [2026-08-21] 調色盤定案：DB32 為風格指引非硬約束，每張素材獨立 ≤32 色量化、不做全域 remap
  （實驗證實 remap 劣化）；全域色彩效果留給 shader。素材後處理管線：tile 類用 fuzz 白背去除
  （非 rembg），建築類 rembg＋alpha 閾值化。
- [2026-08-21] 建築佔格與素材對齊：lumber-camp/quarry/farm 資料表改 1×1（配 64 寬 sprite）；
  2×2 建築（tavern 等）需 128 寬素材，量產時依 art-bible 尺寸規格。

- [2026-08-21] 建築 sprite 無底座規則：地面一律由遊戲地形 tile 提供，生圖前綴帶
  「building only with no ground base tile」；地形類素材另有 tile 版前綴（見 art-bible）。

- [2026-08-21] 視角定案：Anno 式等距（2:1）；俯視 2D 為保底方案，等距生圖一致性過不了時回報再裁決。
- [2026-08-21] 玩法定案：生產鏈+居民模擬為主、疊加危機事件；塔防與冒險者公會兩者都要；
  中世紀為主+魔法解鎖層+多種族（後兩者為資料驅動疊加層，MVP 不含）。
- [2026-08-21] 技術棧：Phaser 3 + TS + Vite；core 純 TS 決定論、零 Phaser 依賴；數值外部化 JSON。
- [2026-08-21] Git 模式：本 session 採預設 (b)——自動 commit，push/PR/merge 前經使用者確認。
